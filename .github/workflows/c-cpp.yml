name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

 script: 
   - sudo docker build -f Dockerfile -t samp_ac_v2:latest .
